"use strict"; // treat all JS code as newer version
// alert(3+3) (cannot use alert syntax here, we can only use it browser)
// but there is a different syntax for alert which we cn use in here
   

// Primitve data types 
//  number
// bigint
// string
// symbol
// null
// umdefined
// boolean


let Name = "Prithvi"
let Age = "19"
let IsLoggedIn = "true"

console.log(IsLoggedIn)